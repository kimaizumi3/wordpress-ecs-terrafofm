---
schemaVersion: '2.2'
description: setting nat-instance
parameters: {}
mainSteps:
- action: aws:runShellScript
  name: ip tables
  inputs:
    runCommand:
    - sudo yum update -y
    - sudo yum install nginx
    - sudo yum install ${package_name}